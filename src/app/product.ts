import { DatePipe } from "@angular/common";

export class Product{
    product_id: number;
    productName: string;
    categoryName: string;
    productDescription: string;
    startDate: Date;
    endDate: Date;
    startBid: number;
   productImage:String;
}

